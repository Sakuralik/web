document.addEventListener('DOMContentLoaded', function () {
    // Get the user agent and display it
    var userAgent = navigator.userAgent;
    document.getElementById('userAgentValue').innerText = userAgent;
});
